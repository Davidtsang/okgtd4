<h1>New binder</h1>
<%= form_for(@stuff, :url=>{:action => 'create_binder'}) do |f| %>
    <% if @stuff.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@stuff.errors.count, "error") %> prohibited this stuff from being saved:</h2>

          <ul>
            <% @stuff.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_area :name %>
    </div>

   <%= f.hidden_field :stuff_type %>

    <div class="actions">
      <%= f.submit '创建', :class => 'btn' %>
    </div>
<% end %>
